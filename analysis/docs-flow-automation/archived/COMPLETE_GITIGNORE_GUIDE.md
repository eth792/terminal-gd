# .spec-workflow 完整 Git 策略指南

> **完整分析**：archive、steering、user-templates 三个目录的 Git 策略

---

## 📁 完整目录结构与用途

```
.spec-workflow/
├── 📂 specs/              ← 核心内容（spec 实施记录）
├── 📂 templates/          ← 默认模板（系统提供）
├── 📂 user-templates/     ← 用户自定义模板
├── 📂 steering/           ← 项目导向文档（product.md, tech.md, structure.md）
├── 📂 archive/            ← 归档的 spec
├── 📂 approvals/          ← MCP 运行时审批数据
└── 📂 .cache/             ← 缓存数据
```

---

## 🔍 逐个目录深度分析

### 1️⃣ user-templates/ - 用户自定义模板

**当前状态**：
```bash
$ ls .spec-workflow/user-templates/
README.md  # 说明文档

$ git ls-files .spec-workflow/user-templates/
.spec-workflow/user-templates/README.md  # ← 已在 git 中
```

**用途**（根据 README.md）：
```markdown
允许用户创建自定义模板，覆盖默认模板

模板加载优先级：
1. user-templates/  ← 优先
2. templates/       ← 回退

示例：
创建 user-templates/requirements-template.md
→ 覆盖 templates/requirements-template.md
```

**性质分析**：

| 维度 | 分析 |
|------|------|
| **内容类型** | 项目特定配置 |
| **可重新生成** | ❌ 无法自动生成 |
| **团队共享** | ✅ 应该共享（统一模板风格）|
| **版本控制价值** | ✅ 高（需要追踪演进）|

**Git 策略**：
```
✅ 应该加入 git

理由：
1. README.md 已在 git 中（作为使用说明）
2. 如果用户创建自定义模板 → 应该追踪（团队统一风格）
3. 模板的演进历史有价值

.gitignore 配置：
# 不需要排除 user-templates/（默认加入）
```

**使用场景**：
```bash
# 场景 1：团队想统一 requirements 格式
$ cat > .spec-workflow/user-templates/requirements-template.md <<EOF
# 需求文档（公司标准格式）

## 业务背景
## 用户故事
## 验收标准
EOF

# 这个文件应该提交到 git，让整个团队使用
$ git add .spec-workflow/user-templates/requirements-template.md
```

---

### 2️⃣ steering/ - 项目导向文档

**当前状态**：
```bash
$ ls .spec-workflow/steering/
# 空目录
```

**用途**（根据 spec-workflow 设计）：
```markdown
存放项目导向文档（Steering Documents）：

1. product.md    - 产品愿景、目标用户、核心价值
2. tech.md       - 技术架构、技术栈、设计原则
3. structure.md  - 项目结构、模块划分、代码组织

目的：
- 为所有 spec 提供统一的项目背景
- AI Agent 可以参考这些文档，理解项目整体方向
```

**性质分析**：

| 维度 | 分析 |
|------|------|
| **内容类型** | 项目级文档（类似 README.md）|
| **可重新生成** | ❌ 无法自动生成 |
| **长期价值** | ✅ 极高（项目架构文档）|
| **版本控制价值** | ✅ 必须追踪 |

**Git 策略**：
```
✅ 应该加入 git

理由：
1. 这是项目的"架构文档"
2. 类似于 docs/ 目录的内容
3. 团队成员和 AI 都需要参考
4. 必须追踪演进历史

.gitignore 配置：
# 不需要排除 steering/（默认加入）
```

**使用场景**：
```bash
# 场景：创建项目导向文档
$ cat > .spec-workflow/steering/tech.md <<EOF
# 技术架构文档

## 技术栈
- Frontend: React + TypeScript
- Backend: Node.js + Express
- Database: PostgreSQL

## 设计原则
1. 优先使用函数式编程
2. 避免过度工程化
3. 单一职责原则
EOF

# 这些文档必须提交到 git
$ git add .spec-workflow/steering/
```

---

### 3️⃣ archive/ - 归档的 spec

**当前状态**：
```bash
$ ls .spec-workflow/archive/
# 空目录
```

**用途**（推测）：
```markdown
存放已完成或废弃的 spec：

使用场景：
1. spec 完成后，从 specs/ 移动到 archive/
2. spec 被废弃，移动到 archive/ 保留记录
3. 清理 specs/ 目录，但保留历史

类比：
类似于 docs/archived/ 或 analysis/archived/
```

**性质分析**：

| 维度 | 分析 | 方案 A | 方案 B |
|------|------|--------|--------|
| **内容类型** | 历史记录 | ✅ | ✅ |
| **访问频率** | 极低 | ❌ | ✅ |
| **长期价值** | 中等（参考价值）| ✅ | ❌ |
| **版本控制** | 可选 | 加入 git | 不加入 git |

**Git 策略（两种方案）**：

#### 方案 A：加入 git（完整历史）
```bash
✅ 优点：
- 完整的项目历史
- 可以追溯废弃的 spec
- 统一管理所有 spec

❌ 缺点：
- 增加 git 仓库体积
- 可能不常访问

适用场景：
- 需要完整的项目历史追溯
- 归档的 spec 仍有参考价值

.gitignore 配置：
# 不排除 archive/
```

#### 方案 B：不加入 git（轻量化）
```bash
✅ 优点：
- 减轻 git 负担
- 归档内容不常访问
- specs/ 中已有完整历史（在 git log 中）

❌ 缺点：
- 无法快速查看归档内容
- 需要从 git log 中恢复

适用场景：
- 归档内容极少访问
- 优先考虑 git 仓库体积

.gitignore 配置：
.spec-workflow/archive/
```

**推荐方案**：**方案 B（不加入 git）**

理由：
```
1. 归档的 spec 在 specs/ 移动前，已经在 git 历史中了
2. 如果需要查看历史 spec → 使用 git log 恢复
3. archive/ 的价值是"本地快速参考"，不是"团队共享"
4. 类比：build/ 目录（构建产物）→ 可重新生成，不加入 git
```

---

## 📊 完整决策表

| 目录 | 用途 | Git 策略 | 理由 |
|------|------|----------|------|
| **specs/** | Spec 实施记录 | ✅ 加入 | 核心内容，AI 代码发现 |
| **templates/** | 默认模板 | ✅ 加入 | 系统资源，已在 git 中 |
| **user-templates/** | 用户自定义模板 | ✅ 加入 | 项目配置，团队共享 |
| **steering/** | 项目导向文档 | ✅ 加入 | 项目架构文档，必须追踪 |
| **archive/** | 归档的 spec | ⚠️ 不加入 | 本地参考，git log 可恢复 |
| **approvals/** | MCP 审批数据 | ❌ 排除 | 运行时数据 |
| **.cache/** | 缓存 | ❌ 排除 | 临时数据 |
| **specs/*/*.backup** | 备份文件 | ❌ 排除 | 自动生成 |

---

## 🎯 最终推荐的 .gitignore 配置

### 精确版（推荐）

```gitignore
# ============================================================
# .spec-workflow MCP Server
# ============================================================

# 排除运行时数据
.spec-workflow/approvals/           # MCP 审批临时数据
.spec-workflow/.cache/              # 缓存文件

# 排除临时备份
.spec-workflow/specs/*/*.backup     # tasks.md.backup 等
.spec-workflow/specs/*/.snapshots/  # 临时快照

# 排除归档（可选，推荐）
.spec-workflow/archive/             # 已归档的 spec（git log 可恢复）

# ============================================================
# 以下目录默认加入 git（不需要配置）：
# - specs/          核心实施记录
# - templates/      默认模板
# - user-templates/ 用户自定义模板
# - steering/       项目导向文档
# ============================================================
```

### 简化版（如果你想追踪 archive/）

```gitignore
# .spec-workflow MCP Server
.spec-workflow/approvals/
.spec-workflow/.cache/
.spec-workflow/specs/*/*.backup
.spec-workflow/specs/*/.snapshots/
```

---

## 📋 实施步骤

### Step 1: 更新 .gitignore

```bash
# 备份当前配置
cp .gitignore .gitignore.backup

# 编辑 .gitignore
vi .gitignore
```

找到：
```gitignore
.spec-workflow/
```

替换为（精确版）：
```gitignore
# ============================================================
# .spec-workflow MCP Server
# ============================================================
.spec-workflow/approvals/
.spec-workflow/.cache/
.spec-workflow/specs/*/*.backup
.spec-workflow/specs/*/.snapshots/
.spec-workflow/archive/              # 可选：如果想追踪 archive/，删除这行
```

### Step 2: 清理并重新添加文件

```bash
# 清理之前可能被错误排除的文件
git rm --cached -r .spec-workflow/ 2>/dev/null || true

# 添加应该追踪的目录
git add .spec-workflow/specs/
git add .spec-workflow/templates/
git add .spec-workflow/user-templates/
git add .spec-workflow/steering/

# 如果想追踪 archive/（可选）
# git add .spec-workflow/archive/
```

### Step 3: 验证

```bash
# 检查哪些文件会被追踪
git status .spec-workflow/

# 应该看到：
✅ .spec-workflow/specs/
✅ .spec-workflow/templates/
✅ .spec-workflow/user-templates/
✅ .spec-workflow/steering/

# 不应该看到：
❌ .spec-workflow/approvals/
❌ .spec-workflow/.cache/
❌ .spec-workflow/archive/
❌ .spec-workflow/specs/*/*.backup
```

### Step 4: 提交

```bash
git add .gitignore
git commit -m "chore: complete .spec-workflow git strategy

Git 策略细化：
- Track specs/ (AI 代码发现)
- Track templates/ (默认模板，已追踪)
- Track user-templates/ (用户自定义模板)
- Track steering/ (项目导向文档)
- Exclude approvals/ (运行时数据)
- Exclude archive/ (本地参考，git log 可恢复)
- Exclude *.backup, .snapshots/ (临时文件)

理由：
- 保留核心内容和配置文档
- 排除运行时和临时数据
- 平衡 git 体积和功能完整性

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 🤔 常见问题

### Q1: archive/ 不加入 git，如何查看历史 spec？

```bash
# 方法 1：从 git 历史中恢复
git log --all --full-history -- .spec-workflow/specs/my-feature/

# 方法 2：查看文件删除前的内容
git show <commit>:.spec-workflow/specs/my-feature/requirements.md

# 方法 3：恢复整个目录到工作区
git checkout <commit> -- .spec-workflow/specs/my-feature/
```

### Q2: 如果我想追踪 archive/ 怎么办？

```bash
# 从 .gitignore 中删除这行：
.spec-workflow/archive/

# 然后添加到 git：
git add .spec-workflow/archive/
git commit -m "chore: track archived specs for complete history"
```

### Q3: steering/ 目录当前为空，也要加入 git 吗？

```bash
# 是的，一旦创建内容就应该追踪

# 创建 .gitkeep 占位（可选）
touch .spec-workflow/steering/.gitkeep
git add .spec-workflow/steering/.gitkeep

# 或者等有实际内容时再添加
```

---

## 💡 Linus 式总结

### 关于 user-templates/

> **"这是用户配置，不是系统数据。团队应该共享同一套模板风格。必须加入 git。"**

### 关于 steering/

> **"这是项目的架构文档，和 README.md 一样重要。不加入 git 就是自杀式行为。"**

### 关于 archive/

> **"归档内容在 git log 里已经有了。archive/ 只是本地快速查看的便利功能，不是版本控制的核心。"**
>
> **"如果你的 archive/ 每天都要查看，说明你的 spec 设计有问题——spec 不应该被归档，应该持续演进。"**

---

## ✅ 最终配置总结

```
加入 git：
✅ specs/          (核心内容)
✅ templates/      (系统资源)
✅ user-templates/ (用户配置)
✅ steering/       (项目文档)

排除：
❌ approvals/      (运行时数据)
❌ archive/        (本地参考，推荐排除)
❌ .cache/         (缓存)
❌ *.backup        (临时文件)
❌ .snapshots/     (临时快照)
```

**简单判断标准**：
- 有长期价值、需要团队共享 → 加入 git
- 运行时数据、临时文件、可重新生成 → 排除

---

**最后更新**: 2025-11-14
**分析师**: Claude (Linus Mode)
**推荐配置**: 精确版（排除 archive/）
